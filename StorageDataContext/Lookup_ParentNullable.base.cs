//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by StorageClassGenerator
//
//     Do not change code in this file, it will get lost when the file gets 
//     auto generated again. Write your code into Lookup_ParentNullable.cs.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
using System;
using System.Collections.Generic;
using System.Threading;
using Storage;


namespace StorageModel  {


    /// <summary>
    /// Parent of children who use lookup, i.e. parent has no children collection,  where the child's 
    /// parent property is nullable.
    /// </summary>
  public partial class Lookup_ParentNullable: IStorage<Lookup_ParentNullable> {

    #region Properties
    //      ----------

    /// <summary>
    /// Unique identifier for Lookup_ParentNullable. Gets set once Lookup_ParentNullable gets added to DC.Data.
    /// </summary>
    public int Key { get; private set; }
    internal static void SetKey(Lookup_ParentNullable lookup_ParentNullable, int key) { lookup_ParentNullable.Key = key; }


    /// <summary>
    /// Stores only dates but no times.
    ///  </summary>
    public DateTime Date { get; }


    /// <summary>
    /// Stores decimal with 2 digits after comma.
    ///  </summary>
    public decimal SomeValue { get; }


    /// <summary>
    /// Headers written to first line in CSV file
    /// </summary>
    internal static readonly string[] Headers = {"Key", "Date", "SomeValue"};


    /// <summary>
    /// None existing Lookup_ParentNullable
    /// </summary>
    internal static Lookup_ParentNullable NoLookup_ParentNullable = new Lookup_ParentNullable(DateTime.MinValue.Date, Decimal.MinValue, isStoring: false);
    #endregion


    #region Events
    //      ------

    /// <summary>
    /// This event will never be raised, but is needed to comply with IStorage.
    /// </summary>
#pragma warning disable 67
    public event Action<Lookup_ParentNullable>? HasChanged;
#pragma warning restore 67
    #endregion


    #region Constructors
    //      ------------

    /// <summary>
    /// Lookup_ParentNullable Constructor. If isStoring is true, adds Lookup_ParentNullable to DC.Data.Lookup_ParentNullables.
    /// </summary>
    public Lookup_ParentNullable(DateTime date, decimal someValue, bool isStoring = true) {
      Key = StorageExtensions.NoKey;
      Date = date.Floor(Rounding.Days);
      SomeValue = someValue.Round(2);
      onConstruct();

      if (isStoring) {
        Store();
      }
    }
    partial void onConstruct();


    /// <summary>
    /// Constructor for Lookup_ParentNullable read from CSV file
    /// </summary>
    private Lookup_ParentNullable(int key, CsvReader csvReader, DC context) {
      Key = key;
      Date = csvReader.ReadDate();
      SomeValue = csvReader.ReadDecimal();
      onCsvConstruct(context);
    }
    partial void onCsvConstruct(DC context);


    /// <summary>
    /// New Lookup_ParentNullable read from CSV file
    /// </summary>
    internal static Lookup_ParentNullable Create(int key, CsvReader csvReader, DC context) {
      return new Lookup_ParentNullable(key, csvReader, context);
    }
    #endregion


    #region Methods
    //      -------

    /// <summary>
    /// Adds Lookup_ParentNullable to DC.Data.Lookup_ParentNullables. 
    /// </summary>
    public void Store() {
      if (Key>=0) {
        throw new Exception($"Lookup_ParentNullable cannot be stored again in DC.Data, key is {Key} greater equal 0." + Environment.NewLine + ToString());
      }
      onStore();
      DC.Data.Lookup_ParentNullables.Add(this);
    }
    partial void onStore();


    /// <summary>
    /// Maximal number of UTF8 characters needed to write Lookup_ParentNullable to CSV file
    /// </summary>
    public const int MaxLineLength = 23;


    /// <summary>
    /// Write Lookup_ParentNullable to CSV file
    /// </summary>
    internal static void Write(Lookup_ParentNullable lookup_ParentNullable, CsvWriter csvWriter) {
      lookup_ParentNullable.onCsvWrite();
      csvWriter.WriteDate(lookup_ParentNullable.Date);
      csvWriter.WriteDecimal2(lookup_ParentNullable.SomeValue);
    }
    partial void onCsvWrite();


    /// <summary>
    /// Removes Lookup_ParentNullable from DC.Data.Lookup_ParentNullables.
    /// </summary>
    public void Remove() {
      if (Key<0) {
        throw new Exception($"Lookup_ParentNullable.Remove(): Lookup_ParentNullable 'Class Lookup_ParentNullable' is not stored in DC.Data, key is {Key}.");
      }
      onRemove();
      DC.Data.Lookup_ParentNullables.Remove(Key);
    }
    partial void onRemove();


    /// <summary>
    /// Returns property values
    /// </summary>
    public string ToShortString() {
      var returnString =
        $"{Key.ToKeyString()}," +
        $" {Date.ToShortDateString()}," +
        $" {SomeValue}";
      onToShortString(ref returnString);
      return returnString;
    }
    partial void onToShortString(ref string returnString);


    /// <summary>
    /// Returns all property names and values
    /// </summary>
    public override string ToString() {
      var returnString =
        $"Key: {Key}," +
        $" Date: {Date.ToShortDateString()}," +
        $" SomeValue: {SomeValue};";
      onToString(ref returnString);
      return returnString;
    }
    partial void onToString(ref string returnString);
    #endregion
  }


  #region Lookup_ParentNullableWriter
  //      ---------------------------

  /// <summary>
  /// Writes a CSV file containing records which can be read back as Lookup_ParentNullable. Note that the keys of linked objects
  /// need to be provided in Write(), since the data context will not be involved.
  /// </summary>
  public class Lookup_ParentNullableWriter: IDisposable {

    readonly CsvConfig csvConfig;
    readonly CsvWriter csvWriter;
    int lastKey = int.MinValue;


    /// <summary>
    /// Constructor, will write the Lookup_ParentNullable header line into the CSV file. Dispose Lookup_ParentNullableWriter once done.
    /// </summary>
    public Lookup_ParentNullableWriter(string? fileNamePath, CsvConfig csvConfig, int maxLineCharLenght) {
      this.csvConfig = csvConfig;
      csvWriter = new CsvWriter(fileNamePath, csvConfig, maxLineCharLenght, null, 0);
      var csvHeaderString = Csv.ToCsvHeaderString(Lookup_ParentNullable.Headers, csvConfig.Delimiter);
      csvWriter.WriteLine(csvHeaderString);
    }


    /// <summary>
    /// Writes the details of one Lookup_ParentNullable to the CSV file
    /// </summary>
    public void Write(int key, DateTime date, decimal someValue) {
      if (key<0) {
        throw new Exception($"Lookup_ParentNullable's key {key} needs to be greater equal 0.");
      }
      if (key<=lastKey) {
        throw new Exception($"Lookup_ParentNullable's key {key} must be greater than the last written Lookup_ParentNullable's key {lastKey}.");
      }
      lastKey = key;
      csvWriter.WriteFirstLineChar(csvConfig.LineCharAdd);
      csvWriter.Write(key);
      csvWriter.WriteDate(date);
      csvWriter.WriteDecimal2(someValue);
      csvWriter.WriteEndOfLine();
    }


    #region IDisposable Support
    //      -------------------

    /// <summary>
    /// Executes disposal of Lookup_ParentNullableWriter exactly once.
    /// </summary>
    public void Dispose() {
      Dispose(true);

      GC.SuppressFinalize(this);
    }


    /// <summary>
    /// Is Lookup_ParentNullableWriter already exposed ?
    /// </summary>
    protected bool IsDisposed {
      get { return isDisposed==1; }
    }


    int isDisposed = 0;


    /// <summary>
    /// Inheritors should call Dispose(false) from their destructor
    /// </summary>
    protected void Dispose(bool disposing) {
      var wasDisposed = Interlocked.Exchange(ref isDisposed, 1);//prevents that 2 threads dispose simultaneously
      if (wasDisposed==1) return; // already disposed

      csvWriter.Dispose();
    }
    #endregion
  }
  #endregion
}
